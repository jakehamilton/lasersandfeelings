import Loading from "../Loading";

const PageLoading = () => {
	return (
		<div class="w-full h-full flex items-center justify-center">
			<Loading class="transform scale-300" />
		</div>
	);
};

export default PageLoading;
